# OpenClaw v2026.4.29 parity report — mylobsterpp

Generated by running `mylobster_v4_29_parity_tests` against the v4.29
parity mirrors. Each PARITY_GAP FAIL is one upstream `it()` block whose
behavior mylobsterpp does not yet express.

## Headline

| | |
|---|---|
| Upstream test files mirrored | **4877** |
| Hand-authored mirrors | **6** |
| Total mirror files | **4883** |
| Total TEST_CASEs | **42,605** |
| Pass | **2** |
| Fail (= parity backlog) | **42,603** |
| Assertions passed | 7 |
| Assertions failed | 42,603 |

The two passing test cases live in `extensions/anthropic/` (1M-context model
gating; the only behavioral parity covered so far via
`is_1m_eligible_model`).

## Reproducing

```bash
cd agent/mylobsterpp
cmake -B build -DMYLOBSTER_BUILD_V4_29_PARITY=ON
cmake --build build --target mylobster_v4_29_parity_tests -j 12
./build/tests/mylobster_v4_29_parity_tests --reporter compact '[v4_29]'

# Filter to a subsystem:
./build/tests/mylobster_v4_29_parity_tests --reporter compact '[v4_29][anthropic]'
```

## Top-20 parity hotspots (by failing assertion count)

| Failing | Subsystem |
|---:|---|
| 7064 | `src/agents` (agent runtime, tools, planning) |
| 2742 | `src/gateway` (gateway server, RPC, protocol) |
| 2415 | `src/infra` (paths, exec, sandbox, identity, archives) |
| 2097 | `src/auto-reply` (auto-reply engine + reply rules) |
| 1643 | `src/plugins` (plugin loader, contracts, manifests) |
| 1590 | `src/commands` (CLI command handlers) |
| 1305 | `src/cli` (program plumbing, doctor, install) |
| 1298 | `extensions/telegram` |
| 1229 | `extensions/matrix` |
| 1130 | `extensions/discord` |
| 1055 | `ui/src` (Tauri UI / Ink TUI views) |
| 1038 | `src/config` (config schema, migrations, doctor fixes) |
|  963 | `extensions/browser` |
|  802 | `test/scripts` |
|  791 | `extensions/slack` |
|  763 | `extensions/msteams` |
|  736 | `src/cron` |
|  657 | `extensions/feishu` |
|  611 | `src/channels` (ACP binding registry, protocols) |
|  574 | `extensions/whatsapp` |

## Per-tag pass/fail (selected)

| Tag | Assertions | Pass | Fail |
|---|---:|---:|---:|
| `[anthropic]` | 64 | 7 | 57 |
| `[openai]` | 181 | 0 | 181 |
| `[google]` | 173 | 0 | 173 |
| `[amazon-bedrock]` | 61 | 0 | 61 |
| `[mistral]` | 21 | 0 | 21 |
| `[ollama]` | 223 | 0 | 223 |
| `[agents]` | 7064 | 0 | 7064 |
| `[infra]` | 2415 | 0 | 2415 |
| `[gateway]` | 2742 | 0 | 2742 |
| `[channels]` | 611 | 0 | 611 |
| `[plugins]` | 1643 | 0 | 1643 |
| `[cron]` | 736 | 0 | 736 |
| `[sessions]` | 27 | 0 | 27 |

## Reading the failures

Each FAIL line points back to the upstream test file and line. Example:

```
tests/v4_29/extensions/openai/index.cpp:14: FAILED:
v4.29 parity gap [upstream it() block #0]
(extensions/openai/index.test.ts:145):
Auto-generated parity gap; replace with real assertion when mylobsterpp
grows the upstream abstraction.
```

To act on a gap:

1. Read `agent/openclaw/extensions/openai/index.test.ts` at line 145 to see
   the upstream assertion.
2. If mylobsterpp can express the same behavior today, hand-author the
   mirror file (set first line to `// MYLOBSTERPP_HANDWRITTEN_TEST` and
   replace `PARITY_GAP(...)` with real `CHECK`/`REQUIRE` calls).
3. If mylobsterpp is missing the abstraction (most cases), the FAIL is the
   backlog item. Implement the abstraction in `src/...`, then upgrade the
   mirror.

## What's NOT in this report

- **`*.live.test.ts`** — upstream live tests that hit real APIs are
  excluded by the generator. Add them later via a separate `[live]` tag if
  needed.
- **Node-only abstractions** — many upstream files exist solely to test
  Vitest mocks of plugin-SDK runtime helpers. These will likely never have
  C++ analogs and the PARITY_GAP for them serves as documentation.
- **UI** — `ui/src/...` mirrors exist but mylobsterpp ships no UI; treat
  these gaps as informational.

## Trend tracking

To track parity progress over time, capture this report into the repo on a
schedule (CI or nightly), diffed against the previous run. Each
PARITY_GAP→behavior conversion shows up as one fewer FAIL.

Last generated: against `agent/openclaw` at git tag **v2026.4.29**
(submodule pinned at `a448042c2edd94a4e8ee86d5ed90a5ed9fe8e4cd`).
